#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	const char *MyMessage = "Programming is like building a multilingual puzzle";

	puts(MyMessage);

	return (0);
}
