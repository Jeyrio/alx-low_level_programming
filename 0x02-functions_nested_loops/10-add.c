#include "main.h"
/**
 * add - Function that adds two integers
 * @n: The character to be checked
 * @m: The character to be checked
 * Return: The result
 *
 */
int add(int n, int m)
{
	int add;

	add = n + m;

	return (add);
}
