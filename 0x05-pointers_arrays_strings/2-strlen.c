#include "main.h"
#include <string.h>

/**
 * _strlen - Function that returns the length of a string
 *
 * @s: Pointer to a char
 *
 * Return: The length of a string
 *
 */
int _strlen(char *s)
{
	return (strlen(s));
}
