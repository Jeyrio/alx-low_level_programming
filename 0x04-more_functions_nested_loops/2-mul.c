#include "main.h"

/**
 * mul - Function that multiplies two integers
 * @a: The integers to be checked
 * @b: The integers to be checked
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
